import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { <%= entiryImport %> } from '<%= entityPath %>';
import { BaseService } from 'src/common/base/base.service';

@Injectable()
export class <%= className %>Service extends BaseService<<%= entiryImport %>> {
  constructor(
    @InjectRepository(<%= entiryImport %>)
    repo: Repository<<%= entiryImport %>>,
  ) {
    super(repo);
  }
}
